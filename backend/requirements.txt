fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
python-dotenv==1.0.1
python-multipart==0.0.9
pypdf==4.3.1
chromadb==0.5.5
requests==2.32.3

# Optional (enable EMBEDDING_PROVIDER=sentence-transformers for higher
# quality embeddings; requires internet access on first run to download
# the model weights). Uncomment to use:
# sentence-transformers==3.0.1
